# Ja'zel — Child scene

Source: `Glenmoril.esm:00C961` — `zzzGHMq05ScJazelChild`

The raw plugin contains a dedicated Ja'zel/Child SCEN record distinct from `JazelBoy` and `JazelMad`.

## Ontology rule

Do not assume “Child” and “Boy” are the same actor solely from adjacent EDIDs. Preserve them as distinct scene concepts until their Actor IDs, aliases and linked dialogue are resolved.

This distinction is important because GLENMORIL repeatedly uses dreams, memories, substitutions and altered identities.
